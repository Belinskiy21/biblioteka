<nav class="navbar navbar-dark bg-dark">
  <ul class="nav navbar-nav">

    <li class="nav-item active">
      <h2><%= link_to 'Biblioteka', root_path, class:"text-white", style:"text-decoration: none" %></h2>
    </li>
  </ul>
  <div class="row">
    <div class="col-lg-12">
    <% if current_user %>
    <span class="text-muted">
      Signed in as <strong class="text-danger"><%= current_user.name%></strong>
      <%= link_to "Sign out", signout_path, id: "sign_out", style:"text-decoration: none" %>
    </span>
    <% else %>
      <%= link_to "Sign in with Google", "/auth/google_oauth2", id: "sign_in", style:"text-decoration: none" %>
    <% end %>
    </div>
  </div>
</nav>
